@Library("tdr-jenkinslib@lambda-failure-alert") _

lambdaTests(
  // Hard-code stage, since it is arbitrary in the lambdaTests
  // function, which publishes the code to the management account
  stage: "mgmt",
  libraryName: "ecr-scan-notifications",
  deployJobName: "TDR ECR Scan Notifications Deploy"
)
